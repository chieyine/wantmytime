// The workspace is private and shows every time in the viewer's own timezone,
// so it renders in the browser. Each page's load function still fetches its
// data before the page shows, and links preload that data on hover.
export const ssr = false;
